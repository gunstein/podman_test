{{- $apps := required "Render shared-proxy through render-kube-runtime.sh (apps registry required)" .Values.apps -}}
{{- $hostnames := list -}}
{{- range $apps -}}
{{- $hostnames = append $hostnames (ternary $.Values.runtime.publicHostname .hostname .identity) -}}
{{- end -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-nginx-data
  annotations:
    volume.podman.io/uid: "101"
    volume.podman.io/gid: "101"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-nginx-env
data:
  TODO_TLS_HOSTNAME: {{ .Values.runtime.publicHostname | quote }}
  APP_TLS_HOSTNAMES: {{ join " " $hostnames | quote }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-nginx-config
data:
  nginx.conf: |
    worker_processes auto;
    error_log /dev/stderr notice;
    pid /tmp/nginx.pid;

    events { worker_connections 1024; }

    http {
        include /etc/nginx/mime.types;
        default_type application/octet-stream;
        access_log /dev/stdout;
        client_body_temp_path /tmp/client_temp;
        proxy_temp_path /tmp/proxy_temp;
        fastcgi_temp_path /tmp/fastcgi_temp;
        uwsgi_temp_path /tmp/uwsgi_temp;
        scgi_temp_path /tmp/scgi_temp;
        sendfile on;
        keepalive_timeout 65;
        server_tokens off;

        include /tmp/podman-resolver.conf;
        upstream shared_keycloak { zone shared_keycloak 64k; server keycloak:8080 resolve; }
{{ range $apps }}
        upstream {{ .name }}_frontend { zone {{ .name }}_frontend 64k; server {{ .frontend }} resolve; }
        upstream {{ .name }}_backend { zone {{ .name }}_backend 64k; server {{ .backend }} resolve; }
{{ end }}
{{ range $apps }}
        server {
            listen 8080;
            listen 8443 ssl;
            server_name {{ ternary $.Values.runtime.publicHostname .hostname .identity }};
            ssl_certificate /var/lib/todo-tls/server.crt;
            ssl_certificate_key /var/lib/todo-tls/server.key;
            ssl_protocols TLSv1.2 TLSv1.3;
            ssl_session_tickets off;
            root /usr/share/nginx/html;
            index index.html;
            add_header X-Content-Type-Options nosniff always;

            location /auth/ {
                proxy_pass http://shared_keycloak;
                include /etc/nginx/todo-proxy-headers.conf;
            }
            location /api/ {
                proxy_pass http://{{ .name }}_backend;
                include /etc/nginx/todo-proxy-headers.conf;
            }
            location = /health {
                proxy_pass http://{{ .name }}_backend;
                include /etc/nginx/todo-proxy-headers.conf;
            }
            location = /ready {
                proxy_pass http://{{ .name }}_backend;
                include /etc/nginx/todo-proxy-headers.conf;
            }
            location / {
                proxy_pass http://{{ .name }}_frontend;
                include /etc/nginx/todo-proxy-headers.conf;
            }
        }
{{ end }}
    }
---
apiVersion: v1
kind: Pod
metadata:
  name: shared-proxy
  labels:
    app.kubernetes.io/name: shared-proxy
    app.kubernetes.io/component: proxy
  annotations:
    io.podman.annotations.pids-limit/nginx: "128"
spec:
  restartPolicy: Never
  containers:
    - name: nginx
      image: {{ .Values.proxy.image | quote }}
      imagePullPolicy: Never
      args: [nginx, -c, /etc/todo-nginx/nginx.conf, -g, "daemon off;"]
      ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
      envFrom:
        - configMapRef:
            name: shared-nginx-env
            optional: false
      volumeMounts:
        - name: nginx-config
          mountPath: /etc/todo-nginx
          readOnly: true
        - name: tls-data
          mountPath: /var/lib/todo-tls
      livenessProbe:
        exec:
          command: [nginx, -t, -c, /etc/todo-nginx/nginx.conf]
        initialDelaySeconds: 10
        periodSeconds: 10
        timeoutSeconds: 3
        failureThreshold: 3
      resources:
        limits:
          memory: {{ .Values.proxy.memory | quote }}
      securityContext:
        runAsUser: 101
        runAsGroup: 101
        allowPrivilegeEscalation: false
        capabilities:
          drop: [ALL]
  volumes:
    - name: nginx-config
      configMap:
        name: shared-nginx-config
        optional: false
        defaultMode: 0444
    - name: tls-data
      persistentVolumeClaim:
        claimName: todo-nginx-data
